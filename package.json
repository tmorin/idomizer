{
  "name": "idomizer",
  "version": "0.4.1",
  "description": "An HTML template compiler providing an incremental-dom render factory.",
  "keywords": [
    "template",
    "incremental",
    "dom",
    "incremental-dom",
    "virtual",
    "virtual-dom",
    "browserify",
    "webpack",
    "systemjs",
    "jspm",
    "loader",
    "webpack-loader",
    "module",
    "plugin",
    "babel-plugin",
    "systemjs-plugin",
    "transform",
    "browserify-transform"
  ],
  "homepage": "https://tmorin.github.io/idomizer",
  "bugs": {
    "url": "https://github.com/tmorin/idomizer/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tmorin/idomizer.git"
  },
  "license": "MIT",
  "author": {
    "name": "Thibault Morin",
    "url": "https://tmorin.github.io"
  },
  "main": "lib/idomizer.js",
  "jsnext:main": "src/idomizer.js",
  "scripts": {
    "clean": "rimraf .nyc_output && rimraf coverage && rimraf dist && rimraf lib && rimraf *.tgz",
    "lint": "jshint src test",
    "test": "nyc mocha --compilers js:babel-register --recursive",
    "test:watch": "npm test -- --watch",
    "test:coveralls": "nyc report --reporter=text-lcov | coveralls --verbose",
    "check": "npm run lint && npm run test",
    "build:lib": "babel src --out-dir lib",
    "build:umd": "webpack src/idomizer.js dist/idomizer.js --config webpack.config.development.js",
    "build:umd:min": "webpack src/idomizer.js dist/idomizer.min.js --config webpack.config.production.js",
    "build": "npm run build:lib && npm run build:umd && npm run build:umd:min",
    "preversion": "npm run check",
    "version": "npm run build && git add -A dist",
    "postversion": "git push && git push --tags && npm run clean && npm run docs:publish",
    "prepublish": "npm run clean && npm run check && npm run build",
    "docs:clean": "rimraf esdoc",
    "docs:build": "npm run docs:clean && esdoc -c esdoc.json",
    "docs:publish": "npm run docs:build && cd esdoc && git init && git commit --allow-empty -m 'update esdoc' && git checkout -b gh-pages && git add . && git commit -am 'update esdoc, [skip ci]' && git push git@github.com:tmorin/idomizer gh-pages --force && cd .. && npm run docs:clean"
  },
  "dependencies": {
    "browserify-transform-tools": "^1.5.0",
    "htmlparser2": "^3.8.3",
    "loader-utils": "^0.2.11"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-core": "^6.0.0",
    "babel-loader": "^6.0.0",
    "babel-plugin-__coverage__": "^0.111111.1",
    "babel-preset-es2015": "^6.0.0",
    "babel-register": "^6.0.0",
    "browserify": "^13.0.0",
    "chai": "^3.4.0",
    "coveralls": "^2.11.9",
    "esdoc": "^0.4.0",
    "incremental-dom": "^0.4.0",
    "jsdom": "^8.0.0",
    "jshint": "^2.8.0",
    "json-loader": "^0.5.0",
    "mocha": "^2.3.0",
    "nyc": "^6.4.0",
    "rimraf": "^2.4.0",
    "systemjs": "^0.19.0",
    "webpack": "^1.12.0",
    "webpack-dev-server": "^1.12.0"
  },
  "npmName": "idomizer",
  "npmFileMap": [
    {
      "basePath": "/dist/",
      "files": [
        "*.js"
      ]
    }
  ]
}
